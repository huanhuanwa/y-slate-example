import { SetNodeOperation } from 'slate';
import { SharedType } from '../../model';
/**
 * Applies a setNode operation to a SharedType
 *
 * @param doc
 * @param op
 */
export default function setNode(doc: SharedType, op: SetNodeOperation): SharedType;

import { SplitNodeOperation } from 'slate';
import { SharedType } from '../../model';
/**
 * Applies a split node operation to a SharedType
 *
 * @param doc
 * @param op
 */
export default function splitNode(doc: SharedType, op: SplitNodeOperation): SharedType;
